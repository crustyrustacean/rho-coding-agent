[workspace]
members = [
    "rho",
    "rho-ai",
    "rho-bench",
    "rho-core",
    "rho-eval",
    "rho-highlight",
    "rho-tools",
    "rho-test-helpers",
    "xtask", "rho-ext",
]
resolver = "3"

[workspace.dependencies]
anyhow = "1.0.102"
async-trait = "0.1"
tokio = { version = "1.52.1", features = ["macros", "rt-multi-thread", "process", "io-util"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0.149"
thiserror = "2.0.18"
tree-sitter = "0.26"
futures = "0.3.32"
sha2 = "0.11.0"
toml = { version = "1.0", features = ["parse"] }
which = "8.0"
reqwest = { version = "0.13.2", features = ["json", "stream"] }
tracing = "0.1.44"
bytes = "1"

[workspace.package]
version = "0.58.0"
edition = "2024"
license = "MIT"
repository = "https://github.com/crustyrustacean/rho-coding-agent"
description = "A coding agent written in Rust"
readme = "README.md"
keywords = ["coding-agent", "ai", "cli"]
categories = ["command-line-utilities", "development-tools"]
publish = false

[workspace.lints.rust]
unsafe_code = "forbid"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = 'abort'
strip = true

[workspace.lints.clippy]
pedantic = { level = "warn", priority = -1 }
cargo = { level = "warn", priority = -1 }
missing_docs_in_private_items = "warn"
missing_errors_doc = "warn"
missing_panics_doc = "warn"
module_name_repetitions = "allow"
must_use_candidate = "allow"
multiple_crate_versions = "allow"
unnecessary_literal_bound = "allow"
