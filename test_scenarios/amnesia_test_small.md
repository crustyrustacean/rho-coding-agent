# Amnesia Test (Small)

Memorize this secret code: PLUM-BLOSSOM-8834

You must pass a multi-step test. Follow each step exactly using the `read_file` tool. Do not guess or skip steps — you MUST call the tool for each file.

Step 1: Call `read_file` with path `rho-core/src/config.rs`. Do not proceed until the tool returns.
Step 2: Call `read_file` with path `rho-tools/src/shell.rs`. Do not proceed until the tool returns.
Step 3: Call `read_file` with path `rho-tools/src/files.rs`. Do not proceed until the tool returns.
Step 4: Call `read_file` with path `rho-core/src/message.rs`. Do not proceed until the tool returns.
Step 5: Call `read_file` with path `rho-core/src/context_files.rs`. Do not proceed until the tool returns.
Step 6: Call `read_file` with path `rho-core/src/redact.rs`. Do not proceed until the tool returns.

Step 7: Output only the secret code. Nothing else.
